using System;
using System.Net;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Threading.Tasks;
using Microsoft.AspNetCore.JsonPatch.Operations;
using Microsoft.AspNetCore.Mvc;
using Firehose.Models;
using Firehose.Models.MacAddressProvisioning;
using Firehose.Models.SupplyChainManagement;
using CoreSharedModels;

public partial interface I{{apiName}}Client {
{{#each paths}}
    Task{{#if responses}}{{#each responses}}{{#if (or customType fieldType)}}<{{#if customType}}{{customType}}{{else}}{{fieldType}}{{/if}}{{#if isArray}}[]{{/if}}?>{{/if}}{{/each}}{{/if}} {{pathName}}({{#each allVariables}}{{#if customType}}{{customType}}{{else}}{{fieldType}}{{/if}}{{#if isArray}}[]{{/if}} {{name}}{{#unless @last}},{{/unless}} {{/each}});
{{/each}}
}