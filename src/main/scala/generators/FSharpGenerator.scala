package com.jfast
package generators

import models.openapi.ApiSpecification

import com.jfast.models.generators.GeneratorResult

class FSharpGenerator extends Generator {
  override def generate(apiSpecification: ApiSpecification): List[GeneratorResult] = {
    List()
  }
}
