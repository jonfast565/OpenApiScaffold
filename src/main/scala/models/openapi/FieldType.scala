package com.jfast
package models.openapi

enum FieldType {
  case None
  case Integer
  case LongInteger
  case DateTime, Object, Custom, Double, TelephoneNumber, Boolean, EmailAddress, String
}
